#include "Dog.hpp"

Dog::Dog( void ) : m_type("Dog") {
  
  std::cout << "Dog default constructor called." << std::endl;

}

// Dog( std::string type );
// Dog( Dog const & other );
// ~Dog( void );

// Dog& operator=( Dog const & other );
// void makeSound( void );